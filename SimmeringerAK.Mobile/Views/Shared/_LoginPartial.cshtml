@if(Request.IsAuthenticated) {
    @Html.ActionLink("Admin", "Index", "Admin", routeValues: null, htmlAttributes: new { data_icon = "gear" })
    @Html.ActionLink(User.Identity.Name, "Index", "Account", routeValues: null, htmlAttributes: new { data_icon = "info" })
}
else {
    @Html.ActionLink("Log in", "Login", "Account", routeValues: null, htmlAttributes: new { data_icon = "info" })
}
